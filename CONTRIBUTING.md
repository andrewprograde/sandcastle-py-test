# Contributing

- Use **Conventional Commits** (`feat:`, `fix:`, `docs:`, `chore:`, …).
- Keep changes focused; one logical change per commit.
- Before opening a PR, the change should run cleanly (this demo repo is small —
  `python calc.py` / any tests present should pass).
- PRs are opened for human review and are **not** auto-merged.
